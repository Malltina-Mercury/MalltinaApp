import React from 'react';
import {SafeAreaView, StatusBar, Text} from 'react-native';

const App: React.FC = () => {
  return (
    <SafeAreaView>
      <StatusBar />
      <Text>Malltina App</Text>
    </SafeAreaView>
  );
};

export default App;
