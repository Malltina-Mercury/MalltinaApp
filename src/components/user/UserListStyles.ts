import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: '90%',
        alignSelf: 'center',
    },
    loading: {
        color: '#fbbf24',
        fontSize: 20,
        fontWeight: '600',
        letterSpacing: 3
    }
});
export default styles;